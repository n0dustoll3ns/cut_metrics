# Progress — Cut Metrics

## Сделано

- ✅ Дизайн-система (токены, компоненты) — `docs/cutmetrics-design-system.html`
- ✅ Code review и рефактор существующей кодовой базы — см. `docs/cut_metrics_project_map.md`
- ✅ Архитектура и ТЗ для фаз 1–5 полностью прописаны:
  - Фаза 1 — модель данных и приоритет источников
  - Фаза 2 — Repository: чтение и запись
  - Фаза 3 — UX-поток подтверждения значения
  - Фаза 4 — без локального кэша
  - Фаза 5 — RecommendationEngine и индикатор источника
- ✅ Реализация Фазы 1 (2026-08-06) — модель данных, резолюция источников, контракт репозитория, мок, 25 тестов
- ✅ Реализация Фазы 2 (2026-08-11) — `HealthRepositoryImpl`, `checkAndRequestPermissions`, `DateKey.startOfDay`/`endOfDay`, `MockHealthRepository` с `recordingMethod`, 13 новых тестов (всего 38)
- ✅ Реализация Фазы 3 (2026-08-11) — `DashboardViewModel`, UI-слой (графики fl_chart, карточка метрики 5 состояний, экраны Today/Dashboard), дизайн-система (токены, google_fonts), `computeEma`, 9 новых тестов (всего 47)
- ✅ Реализация Фазы 4 (2026-08-11) — батчевая агрегация шагов (`aggregateExternalStepsForRange` через `getHealthIntervalDataFromTypes`), документация сессионности кешей, 2 новых теста (всего 49)
- ✅ Реализация Фазы 5 (2026-08-21) — `RecommendationEngine` (темп/статус/рекомендация, чистый Dart, нормализация к неделе), беджи источника, 4 вкладки по макетам (Сегодня/Тренд/Саммари/Настройки), сон (`SleepAnalyzer` + ASLEEP-приоритет), активность (шаги×вес×0.0005 + уровни 1–5), `SettingsService` (слайдер темпа 0.3–1.4, дефолт 0.8), все константы в `recommendation_config.dart`. 41 новый тест (всего 90)
- ✅ Починен `flutter build apk` (2026-08-18) — падал из-за OutOfMemoryException в `update_engine_version.ps1` (общая RDP-машина, Gradle просил `-Xmx8G`); в `android/gradle.properties` занижены JVM-аппетиты Gradle (`-Xmx3G`, metaspace 1G, `workers.max=2`), APK собирается (`app-release.apk`, ~49 МБ)
- ✅ Экран дебаг-сообщений для release (2026-08-26) — `DebugLog` (in-memory за сессию, кольцевой буфер 1000, ChangeNotifier) + `DebugLogScreen` (фильтры-чипы по тегам, «Только ошибки», «Копировать всё» через Clipboard); скрытый вход — 5 тапов по подписи версии в «Настройках»; инструментированы `repo`/`vm`/`perm`/`app` (покрыты все 4 техриска), 10 новых тестов (всего 100). Детали — `activeContext.md` → «Журнал отладки»
- ✅ Кнопка перехода в настройки разрешений (2026-08-26) — при отказе разрешений Health Connect на «Сегодня» показывается `PermissionsBanner` с кнопкой в системные настройки приложения (`ACTION_APPLICATION_DETAILS_SETTINGS` через MethodChannel `cut_metrics/app_settings`, без новых зависимостей); при возврате в приложение `WidgetsBindingObserver` → тихая перепроверка `hasPermissions` без диалога → авто-перезагрузка и исчезновение баннера. 4 новых теста (всего 104). Детали — `activeContext.md` → «Баннер разрешений»
- ✅ Исключён `SLEEP_IN_BED` из разрешений (2026-08-28, решение пользователя) — iOS-only тип без маппинга в Health Connect (health 13.3.1/13.3.2): `hasPermissions` всегда false при выданных правах, пакетный `requestAuthorization` молча фейлится без системного диалога → вечный баннер разрешений. Убран из `kSleepTypes` (10→9 стадий сна, всего 13→12 типов), диагностический тест заменён регрессионным («каждый запрашиваемый тип есть в `dataTypeKeysAndroid`»), README и memory bank обновлены. Тесты: 112 зелёных, `flutter analyze` 4 info / 0 errors. Данные сна не затронуты (ASLEEP + DEEP/LIGHT/REM). Детали — `activeContext.md` → «Раздельная проверка разрешений», `systemPatterns.md`

- ✅ Фикс «поштучно все true, но load: permissions не выданы» (2026-08-28, вариант A, решение пользователя) — нативный `requestAuthorization` пакета health 13.3.1/13.3.2 при уже выданных правах всегда запускает активити HC и получает пустой granted-set → трактуется как false. `checkAndRequestPermissions`: тихая предпроверка `checkPermissionsPerType` (всё выдано → запрос не нужен), запрос только при необходимости (один диалог, UX не изменился), возврат `allGranted` после запроса вместо сырого результата. 2 новых теста (всего 114), `flutter analyze` 4 info / 0 errors. Детали — `systemPatterns.md` → «requestAuthorization → false при уже выданных правах», `activeContext.md` → «Раздельная проверка разрешений»
- ✅ Составлены ТЗ Фазы 6 «Стабилизация и полировка» (2026-09-02): задание №1 (дизайн-система и макеты) — `docs/phase6_design_system_and_mockups_task.md`, задание №2 (реализация и багфиксы) — `docs/phase6_implementation_task.md`; решение по формату дат графика записано в `docs/test_report_26-09-02.md`. Ключевые решения: кеш «Ок» на пару (метрика+источник); «Не ок» = постоянный отказ источника (изменение Фазы 3 §5); один источник на метрику (вес/шаги, дефолт «Авто»); шаги — резолюция по сырым точкам вместо aggregate (техриски №2/№4 снимаются с критического пути); тёмная тема «приборная» через ThemeExtension (дефолт — системная); AccessGate перенесён в финальную фазу «Монетизация»

## В работе / предстоит

- ⬜ Проверка Фазы 2+4 на устройстве (4 технических риска — см. `techContext.md` и `activeContext.md`)
- ⬜ Проверка Фазы 3+5 на устройстве (UX-поток, вкладки, саммари, беджи, слайдер, сборка APK)
- ⬜ Фаза 6 «Стабилизация и полировка» — реализация по `docs/phase6_implementation_task.md` (багфиксы отчёта `test_report_26-09-02.md`, кеш подтверждений, источники, тёмная тема)
- ⬜ (Будущее) Фаза «Монетизация» (AccessGate/paywall, бывшая «Фаза 6») — не начинать без отдельного запроса

## Известные открытые параметры (не блокируют, но требуют подтверждения перед релизом)

- ~~Величина допуска для статуса "в темпе"~~ — ✅ подтверждён пользователем: ±0.15 п.п. (2026-08-21)
- ~~Числа трёх пресетов темпа~~ — ✅ заменены слайдером 0.3–1.4% (шаг 0.1, дефолт 0.8%) по решению пользователя (2026-08-21)
- Формула калорий (шаги×вес×0.0005) и коэффициенты уровней 1–5 (0/1.5/3.0/4.5/6.0 ккал/кг/день) — предложены агентом, приняты пользователем без правок; при калибровке менять только `recommendation_config.dart` / `activity_level.dart`
- Четыре технических риска, требующих проверки на устройстве — см. `techContext.md`

## Как обновлять этот файл

После завершения фазы — переместить пункт из "В работе" в "Сделано", отметить дату. При команде
"update memory bank" — актуализировать этот файл и `activeContext.md` вместе.