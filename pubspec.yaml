name: health_widgets
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.10.7

dependencies:
  collection: ^1.19.1
  fl_chart: ^0.69.2
  flutter:
    sdk: flutter
  health: ^13.3.1
  home_widget: ^0.9.1
  path_provider: ^2.1.5
  provider: ^6.1.5+1
  workmanager: ^0.9.0+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
